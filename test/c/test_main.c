#include "qiskit.h"

int main()
{
    // SparseObservable *obs = obs_zero(100);
    // obs_print(obs);
    // obs_deallocate(obs);
    // BitTerm *bit = bit_term(uin1);
    // bit_term_deallocate(bit);
    enum BitTerm bit;
    bit = Y;
    return 0;
}